# Django secret key (use a secure random string in production)
SECRET_KEY=django-insecure-12345fakeexamplekey!@#$%^&*

# Debug mode (set to False in production)
DEBUG=True

# Allowed hosts (comma-separated, leave empty for development)
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL database configuration
POSTGRES_DB=theatre_demo
POSTGRES_USER=demo_user
POSTGRES_PASSWORD=demo_password
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Admin email (optional, can be used for notifications)
ADMIN_EMAIL=admin@example.com
